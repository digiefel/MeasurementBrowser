interface:
  display_name: "DataBrowser Profiling"
  short_description: "Live Revise profiling with ProfileView and pprof"
  default_prompt: "Use $databrowser-profiling in one live Revise session. Keep the GUI workspace open, profile the requested behavior, save the pprof text views, and link the inspectable output files with a short plain-language explanation."
