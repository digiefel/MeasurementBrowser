interface:
  display_name: "DataBrowser Profiling"
  short_description: "timed_profile.jl harness for RuO2 cold-cache scan performance"
  default_prompt: "Follow $databrowser-profiling: sync-check, one gui harness run, return full stdout and artifact paths."
